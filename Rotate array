var rotate = function(nums, k) {
    var n=nums.length;
    k=k%n;
    var a=[];
    for(let i=n-k;i<n;i++){
        a.push(nums[i]);
    }
    for(let i=0;i<n-k;i++){
        a.push(nums[i]);
    }
    for(let i=0;i<n;i++){
        nums[i]=a[i];
    }
    
};
